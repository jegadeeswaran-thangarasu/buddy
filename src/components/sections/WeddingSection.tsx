/* PHOTO SECTION — commented out temporarily, will restore later */
"use client";

// All imports commented out — restore with photos
// import { useEffect, useRef, useState } from "react";
// import { motion, useInView } from "framer-motion";
// import { useSectionAudio } from "@/hooks/useSectionAudio";
// import FloatingPhoto from "@/components/ui/FloatingPhoto";
// import Lightbox from "@/components/ui/Lightbox";
// import { weddingPhotos, type PhotoItem } from "@/data/photos";

interface Props { onSectionComplete?: () => void }

// TEMPORARILY DISABLED — photos coming soon
export default function WeddingSection(_props: Props) {
  return null;
}
